import {AbbreviateShaPipe} from './abbreviate-sha.pipe';

describe('AbbreviateShaPipe', () => {
  it('create an instance', () => {
    const pipe = new AbbreviateShaPipe();
    expect(pipe).toBeTruthy();
  });
});
